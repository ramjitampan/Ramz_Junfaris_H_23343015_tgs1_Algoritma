{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Raylib",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/game.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Build C Raylib"
        }
    ]
}
